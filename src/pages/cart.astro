---
import Layout from '../layouts/Layout.astro';
// import cartStore from '../stores/cart';

// Get current cart state
// const cartState = cartStore.getState();
// const { items, total, isLoading, error } = cartState;

// const calculateSubtotal = (items: typeof cartState.items) => {
//   return items.reduce((total, item) => total + (parseFloat(item.price) * item.quantity), 0).toFixed(2);
// };

// const subtotal = calculateSubtotal(items);
// const shipping = items.length > 0 ? 9.99 : 0;
// const tax = parseFloat(subtotal) * 0.08; // 8% tax
// const calculatedTotal = (parseFloat(subtotal) + shipping + tax).toFixed(2);
---

<Layout title="Shopping Cart">
  <lasz-cart></lasz-cart>
</Layout>

<script>
  import '../elements/lasz-cart';
  // import cartStore from '../stores/cart';

  // // Subscribe to cart changes
  // const unsubscribe = cartStore.subscribe((state) => {
  //   updateCartDisplay(state);
  // });

  // function updateCartDisplay(state) {
  //   // Update loading state
  //   const loadingElements = document.querySelectorAll('.loading');
  //   loadingElements.forEach(el => {
  //     el.style.display = state.isLoading ? 'block' : 'none';
  //   });

  //   // Update error state
  //   const errorElements = document.querySelectorAll('.error');
  //   errorElements.forEach(el => {
  //     el.style.display = state.error ? 'block' : 'none';
  //   });

  //   // Update cart items count
  //   const itemCount = document.querySelector('.item-count');
  //   if (itemCount) {
  //     itemCount.textContent = `${state.items.length} ${state.items.length === 1 ? 'item' : 'items'}`;
  //   }

  //   // Force page reload if cart structure changes significantly
  //   if (state.items.length !== parseInt(itemCount?.textContent?.split(' ')[0] || 0)) {
  //     window.location.reload();
  //   }
  // }

  // // Add interactivity for cart actions
  // document.addEventListener('DOMContentLoaded', () => {
  //   // Fetch initial cart data
  //   cartStore.getState().fetchCart();

  //   // Quantity change handlers
  //   const quantityInputs = document.querySelectorAll('.quantity-input');
  //   quantityInputs.forEach(input => {
  //     input.addEventListener('change', async (e) => {
  //       const target = e.target;
  //       const key = target.dataset.key;
  //       const newQuantity = parseInt(target.value);

  //       if (newQuantity < 1) {
  //         target.value = 1;
  //         return;
  //       }

  //       try {
  //         await cartStore.getState().updateQuantity(key, newQuantity);
  //         // Refresh the page to show updated totals
  //         setTimeout(() => window.location.reload(), 500);
  //       } catch (error) {
  //         console.error('Failed to update quantity:', error);
  //         target.value = target.defaultValue; // Reset to original value
  //       }
  //     });
  //   });

  //   // Remove item handlers
  //   const removeButtons = document.querySelectorAll('.remove-btn');
  //   removeButtons.forEach(button => {
  //     button.addEventListener('click', async (e) => {
  //       const target = e.target;
  //       const key = target.dataset.key;

  //       if (confirm('Are you sure you want to remove this item?')) {
  //         try {
  //           await cartStore.getState().removeFromCart(key);
  //           // Refresh the page to show updated cart
  //           setTimeout(() => window.location.reload(), 500);
  //         } catch (error) {
  //           console.error('Failed to remove item:', error);
  //         }
  //       }
  //     });
  //   });

  //   // Checkout button
  //   const checkoutBtn = document.querySelector('.checkout-btn');
  //   if (checkoutBtn) {
  //     checkoutBtn.addEventListener('click', () => {
  //       // TODO: Redirect to checkout page
  //       window.location.href = '/checkout';
  //     });
  //   }

  //   // Continue shopping button
  //   const continueButtons = document.querySelectorAll('.continue-shopping-btn');
  //   continueButtons.forEach(button => {
  //     button.addEventListener('click', () => {
  //       // TODO: Redirect to shop page
  //       window.location.href = '/products';
  //     });
  //   });
  // });

  // // Cleanup subscription on page unload
  // window.addEventListener('beforeunload', unsubscribe);
</script>
